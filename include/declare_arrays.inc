//declare layers
vector<vector<float>> dem = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<short int>> texture = vector<vector<short int>>(xSize,vector<short int> (ySize,0));
vector<vector<short int>> landuse = vector<vector<short int>>(xSize,vector<short int> (ySize,0));
vector<vector<short int>> vegetation = vector<vector<short int>>(xSize,vector<short int> (ySize,0));
vector<vector<short int>> channel = vector<vector<short int>>(xSize,vector<short int> (ySize,0));
vector<vector<int>> lakes = vector<vector<int>>(xSize,vector<int> (ySize,0));
vector<vector<float>> slope = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> SD = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<vector<float>>> SW = vector<vector<vector<float>>>( xSize, vector<vector<float> >(ySize, vector<float>(2, noData)));
vector<vector<vector<float>>> EHDepth = vector<vector<vector<float>>>( xSize, vector<vector<float> >(ySize, vector<float>(2, noData)));
vector<vector<float>> SW_EHD = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> SWSat = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> FC = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> SWSat_EHD = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> CC = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> erod = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> drainageRate1 = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> drainageRate2 = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> dailySurplus = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> toDistribute = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> dsc = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> dsc_max = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> Q_cum = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> k = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> flaccMFD = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> TC = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> TC_red = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> toDistribute_sedi = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> eroMFD = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> depoMFD = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<bool>> gully = vector<vector<bool>>(xSize,vector<bool> (ySize,0));
vector<vector<vector<float> > > d = vector<vector<vector<float> > >( xSize, vector<vector<float> >(ySize, vector<float>(8, noData)));
vector<vector<vector<float> > > dG = vector<vector<vector<float> > >( xSize, vector<vector<float> >(ySize, vector<float>(8, noData)));
vector<vector<float>> latflow = vector<vector<float>>(xSize, vector<float> (ySize,0));
vector<vector<vector<float> > > G = vector<vector<vector<float>>>( xSize, vector<vector<float> >(ySize, vector<float>(2, noData)));
vector<vector<float>> slopeG = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<float>> flaccMAX = vector<vector<float>>(xSize,vector<float> (ySize,0));
vector<vector<short int>> measures = vector<vector<short int>>(xSize,vector<short int> (ySize,0));

for(int x=0;x<xSize;x++)
	{
	for(int y=0;y<ySize;y++)
		{
		dem[x][y]=noData;
		landuse[x][y]=noData;
		texture[x][y]=noData;
		vegetation[x][y]=noData;
		channel[x][y]=noData;
		SD[x][y]=1500;
		SW[x][y][0]=0;
		SW[x][y][1]=0;
		SW_EHD[x][y]=0;
		SWSat[x][y]=0;
		SWSat_EHD[x][y]=0;
		FC[x][y]=0;
		CC[x][y]=0.05;
		erod[x][y]=0;
		EHDepth[x][y][0]=.1;
		EHDepth[x][y][1]=.1;
		drainageRate1[x][y]=1.0;
		drainageRate2[x][y]=1.0;
		dailySurplus[x][y]=0;
		toDistribute[x][y]=0;
		dsc[x][y]=0;
		dsc_max[x][y]=0;
		Q_cum[x][y]=0;
		k[x][y]=noData;
		flaccMFD[x][y]=0;
		TC[x][y]=0;
		TC_red[x][y]=1;
		toDistribute_sedi[x][y]=0;
		eroMFD[x][y]=0;
		depoMFD[x][y]=0;
		gully[x][y]=false;
		latflow[x][y]=0;
		G[x][y][0]=0;
		G[x][y][1]=0;
		slopeG[x][y]=0;
		flaccMAX[x][y]=0;
		measures[x][y]=0;		
		}
	}
